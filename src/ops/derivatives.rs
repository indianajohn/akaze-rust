use types::image::GrayFloatImage;

pub fn scharr(image: &GrayFloatImage, _x_order: u32, _y_order: u32) -> GrayFloatImage {
    warn!("implement scharr derivatives");
    let return_image = image.clone();
    return_image
}
